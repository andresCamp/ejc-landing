import { Questrial, Syne } from 'next/font/google'

export const syne = Syne({ subsets: ['latin'], variable:'--font-syne', weight: ['400','500', '700'] })

export const questrial = Questrial({ subsets: ['latin'], variable:'--font-questrial', weight: ['400'] })